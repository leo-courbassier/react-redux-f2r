import * as types from '../constants/ActionTypes';
import objectAssign from 'object-assign';
import _ from 'underscore';

const initialState = {

};


export default function dasboardAppState(state = initialState, action) {
  switch (action.type) {



    default:
      return state;
  }
}
